export interface User {
  avatar: string;
  avatar_url: string;
  creation: string;
  date: string;
  discriminator: string;
  id: string;
  public_flags: number;
  flags_abstracted: string[];
  request_count: number;
  username: string;
  bot: boolean;
}
